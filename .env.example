# .env.example
# Environment variables template for KIIAREN Platform
# Single source of truth for all apps (web, convex, docs, whiteboard)

# ============================================================================
# CORE PLATFORM (Required)
# ============================================================================

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# Convex Deployment
# Get from: https://convex.dev (Dashboard > Deployments)
# Format: dev:<deployment-name> # team: <team-name>, project: <project-name>
# Used by: apps/web, convex backend
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Convex Public URL
# Get from: https://convex.dev (Dashboard > Deployments)
# Format: https://<deployment-name>.convex.cloud
# Used by: apps/web (client-side), all features (chat, docs, whiteboard)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment-name.convex.cloud

# Site URL (for Convex Auth callbacks)
# Local development: http://localhost:3000
# Production: your actual domain
# Used by: convex/auth.config.ts for OAuth redirects
CONVEX_SITE_URL=http://localhost:3000

# ============================================================================
# AUTHENTICATION (Optional but recommended)
# ============================================================================

# Google OAuth Credentials
# Get from: https://console.cloud.google.com/
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: {CONVEX_SITE_URL}/api/auth/callback/google
# Used by: Convex Auth (login with Google)
# AUTH_GOOGLE_CLIENT_ID=your-google-client-id
# AUTH_GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth Credentials
# Get from: https://github.com/settings/developers
# 1. Create new OAuth App
# 2. Set authorization callback URL: {CONVEX_SITE_URL}/api/auth/callback/github
# Used by: Convex Auth (login with GitHub)
# AUTH_GITHUB_ID=your-github-client-id
# AUTH_GITHUB_SECRET=your-github-client-secret

# ============================================================================
# FEATURE FLAGS (Optional)
# ============================================================================

# Note: Docs and Whiteboard features use Convex _storage for file/image uploads
# No additional storage services (EdgeStore, etc.) are required
